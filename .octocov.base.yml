coverage:
  badge:
    path: .github/octocov/coverage.svg
  paths:
    - coverage/.resultset.json
testExecutionTime:
  badge:
    path: .github/octocov/time.svg
  steps:
    - Exec rspec
codeToTestRatio:
  code:
    - "app/**/*.rb"
  test:
    - "spec/**/*_spec.rb"
report:
  datastores:
    - artifact://${GITHUB_REPOSITORY}/base
