coverage:
  paths:
    - coverage/.resultset.json
testExecutionTime:
  steps:
    - Exec rspec
codeToTestRatio:
  code:
    - "app/**/*.rb"
  test:
    - "spec/**/*_spec.rb"
report:
  datastores:
    - artifact://${GITHUB_REPOSITORY}/base
