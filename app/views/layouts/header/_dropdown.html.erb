<div data-regexp-examples-target="headerDropdown" class="hidden z-50 sm:w-96 w-full max-w-full sm:max-w-sm" role="menu" aria-label="<%= t("regular_expressions.header.examples", default: "Examples") %>">
  <div class="bg-gradient-to-b from-slate-900 to-gray-800 text-gray-100 border border-gray-700 rounded-lg shadow-2xl overflow-hidden ring-1 ring-black/40">
      <!-- Two-column: left = categories, right = items for selected category -->
      <div class="px-2 py-1">
        <div class="flex gap-2">
          <!-- categories column (independent scroll) -->
          <div data-regexp-examples-target="headerScroll" class="w-1/3 border-r border-gray-700 pl-1 pr-3 max-h-80 overflow-auto" style="scrollbar-gutter: stable;">
            <div class="sticky top-0 z-10 bg-slate-900/90 text-xs font-semibold text-gray-400 pl-1 pr-3 border-b border-gray-700 py-1"><%= t("regular_expressions.header.categories_label", default: "Categories") %></div>
            <%# Use lightweight category list to avoid constructing examples on initial render %>
            <% RegularExpression::Example.category_list.each_with_index do |(cat, data), i| %>
              <button type="button"
                class="w-full text-left text-sm text-gray-300 py-1 px-1 hover:bg-gray-700 focus:outline-none transition-colors"
                data-header-category="<%= cat.to_s.parameterize %>"
                data-action="click->regexp-examples#showCategory click->regexp-examples#showHeaderCategory">
                <div class="font-medium"><%= data[:short] %></div>
                <div class="text-xs text-gray-400"><%= cat.to_s.humanize %></div>
              </button>
            <% end %>
          </div>

          <!-- items column -->
            <div data-regexp-examples-target="headerItemsScroll" class="w-2/3 max-h-80 overflow-auto pl-1 pr-3" style="scrollbar-gutter: stable;">
              <div class="sticky top-0 z-10 bg-slate-900/90 text-xs font-semibold text-gray-400 pl-1 pr-3 border-b border-gray-700 py-1"><%= t("regular_expressions.header.examples_label", default: "Examples") %></div>
              <%# Items will be loaded on demand via AJAX when a category is selected. Show an initial prompt. %>
              <div data-header-initial-prompt="true" class="p-3 text-sm text-gray-400"><%= t("regular_expressions.header.open_examples", default: "Open examples (top-right)") %></div>
            </div>
        </div>
      </div>
  </div>
</div>
